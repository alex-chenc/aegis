# Task 1.1: Project Skeleton and Infrastructure Files Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Create the complete project directory structure, build system files (Makefiles, build.sh), Docker configuration (docker-compose.yml, Dockerfiles), database initialization (init.sql), and protobuf definitions for the "Automated Baseline Check and Self-Healing System".

**Architecture:** The project consists of three main components (backend, frontend, agent) with shared infrastructure (PostgreSQL, Redis, MinIO). Each component has its own build system using Makefile + build.sh pattern, with Docker Compose orchestrating all services.

**Tech Stack:** Go 1.20+, Vue 3 + TypeScript + Vite, PostgreSQL 14, Redis 7, MinIO, gRPC, Docker Compose 3.8

**Design References:**
- `baseline_system_design_v2.1/infrastructure_design_v2.1_complete.md` - Infrastructure & Docker
- `baseline_system_design_v2.1/build_system_design_v2.1_complete.md` - Build system
- `baseline_system_design_v2.1/communication_structure_design_v2.1_complete.md` - gRPC/Protobuf definitions
- `baseline_system_design_v2.1/database_structure_design_v2.1_complete.md` - Database schema

---

## Phase 1: Root Level Files

### Task 1.1: Create .env.example

**Files:**
- Create: `/code/ai-benchmark/.env.example`

**Step 1: Create .env.example with all required environment variables**

```bash
# .env - 环境变量配置文件（请勿提交到 Git）

# 数据库
DB_PASSWORD=a_strong_db_password_here
DB_PORT=5432

# Redis
REDIS_PASSWORD=a_strong_redis_password_here
REDIS_PORT=6379

# MinIO
MINIO_ACCESS_KEY=minio_admin
MINIO_SECRET_KEY=a_strong_minio_password_here
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# Agent
AGENT_TOKEN=a_strong_agent_token_here

# 外部访问 IP（用于生成 Agent 安装命令）
# 留空则后端自动检测（优先公网 IP）；填写则直接使用该值
EXTERNAL_IP=
```

**Step 2: Verify file exists**

```bash
ls -la /code/ai-benchmark/.env.example
```

**Step 3: Commit**

```bash
git add .env.example
git commit -m "chore: add .env.example template with all environment variables"
```

---

## Phase 2: Docker Compose and Infrastructure

### Task 1.2: Create docker-compose.yml

**Files:**
- Create: `/code/ai-benchmark/docker-compose.yml`

**Step 1: Create complete docker-compose.yml**

```yaml
# docker-compose.yml
# 自动化基线检查与自愈系统 - 容器编排配置
# 版本：V2.0

version: "3.8"

services:
  # ============================================================
  # 基础设施层
  # ============================================================

  # PostgreSQL 关系型数据库
  postgres:
    image: postgres:14-alpine
    container_name: baseline-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: baseline_db
      POSTGRES_USER: baseline_user
      POSTGRES_PASSWORD: ${DB_PASSWORD:-a_strong_db_password}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=C"
    command:
      - "postgres"
      - "-c"
      - "max_connections=100"
      - "-c"
      - "shared_buffers=256MB"
      - "-c"
      - "effective_cache_size=768MB"
      - "-c"
      - "maintenance_work_mem=128MB"
      - "-c"
      - "checkpoint_completion_target=0.9"
      - "-c"
      - "wal_buffers=16MB"
      - "-c"
      - "default_statistics_target=100"
      - "-c"
      - "random_page_cost=1.1"
      - "-c"
      - "effective_io_concurrency=200"
      - "-c"
      - "work_mem=4MB"
      - "-c"
      - "min_wal_size=1GB"
      - "-c"
      - "max_wal_size=4GB"
      - "-c"
      - "log_statement=mod"
      - "-c"
      - "log_min_duration_statement=1000"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/scripts/init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    ports:
      - "${DB_PORT:-5432}:5432"
    networks:
      - baseline-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U baseline_user -d baseline_db"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # Redis 高速缓存
  redis:
    image: redis:7-alpine
    container_name: baseline-redis
    restart: unless-stopped
    command:
      - "redis-server"
      - "--requirepass"
      - "${REDIS_PASSWORD:-a_strong_redis_password}"
      - "--maxmemory"
      - "256mb"
      - "--maxmemory-policy"
      - "allkeys-lru"
      - "--appendonly"
      - "yes"
      - "--appendfsync"
      - "everysec"
      - "--tcp-keepalive"
      - "60"
      - "--timeout"
      - "300"
      - "--databases"
      - "4"
    volumes:
      - redis_data:/data
    ports:
      - "${REDIS_PORT:-6379}:6379"
    networks:
      - baseline-network
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-a_strong_redis_password}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # MinIO 对象存储
  minio:
    image: minio/minio:latest
    container_name: baseline-minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY:-minio_admin}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY:-a_third_strong_secret_password}
    command: server /data --console-address ":9001"
    volumes:
      - minio_data:/data
    ports:
      - "${MINIO_API_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_PORT:-9001}:9001"
    networks:
      - baseline-network
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  # MinIO 初始化容器（一次性运行，创建 Bucket）
  minio-init:
    image: minio/mc:latest
    container_name: baseline-minio-init
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set myminio http://minio:9000 $${MINIO_ACCESS_KEY} $${MINIO_SECRET_KEY};
      mc mb myminio/baseline-templates --ignore-existing;
      mc mb myminio/agent-artifacts --ignore-existing;
      mc mb myminio/generated-scripts --ignore-existing;
      mc anonymous set download myminio/agent-artifacts;
      echo 'MinIO initialization complete.';
      exit 0;
      "
    environment:
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minio_admin}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-a_third_strong_secret_password}
    networks:
      - baseline-network

  # ============================================================
  # 应用层
  # ============================================================

  # Go 后端服务
  backend:
    image: baseline-system/backend:latest
    container_name: baseline-backend
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
    environment:
      # 数据库配置
      DATABASE_HOST: postgres
      DATABASE_PORT: 5432
      DATABASE_USER: baseline_user
      DATABASE_PASSWORD: ${DB_PASSWORD:-a_strong_db_password}
      DATABASE_DBNAME: baseline_db
      DATABASE_SSLMODE: disable
      DATABASE_MAX_OPEN_CONNS: 25
      DATABASE_MAX_IDLE_CONNS: 10
      DATABASE_CONN_MAX_LIFETIME: 300
      # Redis 配置
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-a_strong_redis_password}
      REDIS_DB: 0
      REDIS_POOL_SIZE: 20
      # MinIO 配置
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minio_admin}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-a_third_strong_secret_password}
      MINIO_USE_SSL: "false"
      # 服务配置
      SERVER_HTTP_PORT: 8080
      SERVER_GRPC_PORT: 9090
      SERVER_EXTERNAL_IP: ${EXTERNAL_IP:-}
      # Agent 配置
      AGENT_AUTH_TOKEN: ${AGENT_TOKEN:-a_very_secret_agent_token}
      AGENT_HEARTBEAT_TIMEOUT: 90
      AGENT_SCRIPT_TIMEOUT: 300
      # 自愈配置
      SELF_HEALING_MAX_RETRIES: 3
      SELF_HEALING_ENABLED: "true"
    ports:
      - "8080:8080"
      - "9090:9090"
    networks:
      - baseline-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s

  # Vue 前端 (Nginx)
  frontend:
    image: baseline-system/frontend:latest
    container_name: baseline-frontend
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy
    ports:
      - "80:80"
    networks:
      - baseline-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:80"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s

# ============================================================
# 网络配置
# ============================================================
networks:
  baseline-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.28.0.0/16

# ============================================================
# 数据卷配置
# ============================================================
volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  minio_data:
    driver: local
```

**Step 2: Verify file exists**

```bash
ls -la /code/ai-benchmark/docker-compose.yml
wc -l /code/ai-benchmark/docker-compose.yml
```

**Step 3: Commit**

```bash
git add docker-compose.yml
git commit -m "chore: add docker-compose.yml with all services and infrastructure"
```

---

## Phase 3: Backend Project Structure

### Task 1.3: Create Backend Directory Structure

**Files:**
- Create directories under `/code/ai-benchmark/backend/`

**Step 1: Create all backend directories**

```bash
cd /code/ai-benchmark/backend

# Create directory structure
mkdir -p cmd/server
mkdir -p config
mkdir -p internal/api/handler
mkdir -p internal/api/middleware
mkdir -p internal/grpc_server
mkdir -p internal/service
mkdir -p internal/repository
mkdir -p internal/llm
mkdir -p internal/fileparser
mkdir -p internal/storage
mkdir -p internal/ipdetect
mkdir -p internal/model
mkdir -p pkg/api/v1
mkdir -p scripts
```

**Step 2: Verify structure**

```bash
tree /code/ai-benchmark/backend -d
```

**Step 3: Commit**

```bash
git add backend/
git commit -m "feat(backend): create project directory structure"
```

---

### Task 1.4: Create Backend Makefile

**Files:**
- Create: `/code/ai-benchmark/backend/Makefile`

**Step 1: Create Makefile**

```makefile
# backend/Makefile

.PHONY: all build clean test docker

# Go 相关变量
BINARY_NAME=backend
GO_CMD=go

# Docker 相关变量
IMAGE_NAME=baseline-system/backend
IMAGE_TAG?=latest

all: build

# 构建 Go 二进制文件
build:
	@echo "Building Go binary..."
	$(GO_CMD) build -o $(BINARY_NAME) ./cmd/server/main.go

# 清理构建产物
clean:
	@echo "Cleaning..."
	$(GO_CMD) clean
	rm -f $(BINARY_NAME)

# 运行测试
test:
	@echo "Running tests..."
	$(GO_CMD) test ./...

# 构建 Docker 镜像（调用 build.sh）
docker:
	@./build.sh $(IMAGE_NAME) $(IMAGE_TAG)
```

**Step 2: Verify and test**

```bash
cd /code/ai-benchmark/backend
cat Makefile
make --dry-run build
```

**Step 3: Commit**

```bash
git add backend/Makefile
git commit -m "chore(backend): add Makefile with build, clean, test, docker targets"
```

---

### Task 1.5: Create Backend build.sh

**Files:**
- Create: `/code/ai-benchmark/backend/build.sh`

**Step 1: Create build.sh**

```bash
#!/bin/bash
# backend/build.sh

set -e

IMAGE_NAME=${1:-"baseline-system/backend"}
IMAGE_TAG=${2:-"latest"}

# 基础镜像
BASE_IMAGE="golang:1.20-alpine"

echo "Building Docker image ${IMAGE_NAME}:${IMAGE_TAG}..."

# 使用基础镜像进行构建
docker build -t ${IMAGE_NAME}:${IMAGE_TAG} \
    --build-arg BASE_IMAGE=${BASE_IMAGE} \
    -f Dockerfile .

echo "Build complete: ${IMAGE_NAME}:${IMAGE_TAG}"
```

**Step 2: Make executable and test**

```bash
chmod +x /code/ai-benchmark/backend/build.sh
ls -la /code/ai-benchmark/backend/build.sh
```

**Step 3: Commit**

```bash
git add backend/build.sh
git commit -m "chore(backend): add build.sh for Docker image building"
```

---

### Task 1.6: Create Backend Dockerfile

**Files:**
- Create: `/code/ai-benchmark/backend/Dockerfile`

**Step 1: Create Dockerfile**

```dockerfile
# backend/Dockerfile

# --- Build Stage ---
ARG BASE_IMAGE=golang:1.20-alpine
FROM ${BASE_IMAGE} AS builder

WORKDIR /app

# 复制 go.mod 和 go.sum 并下载依赖
COPY go.mod go.sum ./
RUN go mod download

# 复制源代码并构建
COPY . .
RUN make build

# --- Final Stage ---
FROM alpine:latest

WORKDIR /root/

# 从 builder 阶段复制二进制文件和配置文件
COPY --from=builder /app/backend .
COPY --from=builder /app/config/config.yaml .

# 暴露端口
EXPOSE 8080
EXPOSE 9090

# 运行命令
CMD ["./backend"]
```

**Step 2: Verify**

```bash
cat /code/ai-benchmark/backend/Dockerfile
```

**Step 3: Commit**

```bash
git add backend/Dockerfile
git commit -m "chore(backend): add multi-stage Dockerfile"
```

---

### Task 1.7: Create Backend config.yaml Template

**Files:**
- Create: `/code/ai-benchmark/backend/config/config.yaml`

**Step 1: Create config.yaml**

```yaml
# backend/config/config.yaml
# 后端服务配置文件

server:
  http_port: 8080
  grpc_port: 9090
  # 外部访问 IP，留空则自动检测
  external_ip: ""

database:
  host: localhost
  port: 5432
  user: baseline_user
  password: your_db_password
  dbname: baseline_db
  sslmode: disable
  max_open_conns: 25
  max_idle_conns: 10
  conn_max_lifetime: 300

redis:
  host: localhost
  port: 6379
  password: your_redis_password
  db: 0
  pool_size: 20

minio:
  endpoint: localhost:9000
  access_key: minio_admin
  secret_key: your_minio_password
  use_ssl: false
  buckets:
    - baseline-templates
    - agent-artifacts
    - generated-scripts

llm:
  # 留空则从环境变量读取
  api_key: ""
  base_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"
  model_name: "qwen-plus"
  timeout_seconds: 60
  max_retries: 3

agent:
  auth_token: your_agent_token
  heartbeat_timeout_seconds: 90
  script_timeout_seconds: 300

self_healing:
  max_retries: 3
  enabled: true
```

**Step 2: Verify**

```bash
cat /code/ai-benchmark/backend/config/config.yaml
```

**Step 3: Commit**

```bash
git add backend/config/config.yaml
git commit -m "chore(backend): add config.yaml template with all configuration options"
```

---

### Task 1.8: Create Database init.sql

**Files:**
- Create: `/code/ai-benchmark/backend/scripts/init.sql`

**Step 1: Create init.sql with complete database schema**

```sql
-- backend/scripts/init.sql
-- 自动化基线检查与自愈系统 - 数据库初始化脚本
-- 版本：V2.0

-- ============================================================
-- 创建扩展
-- ============================================================
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pgcrypto";

-- ============================================================
-- 创建表：hosts (资产表)
-- ============================================================
CREATE TABLE IF NOT EXISTS hosts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    ip_address VARCHAR(45) NOT NULL UNIQUE,
    hostname VARCHAR(255) NOT NULL,
    os_type VARCHAR(50) NOT NULL,
    agent_version VARCHAR(50) NOT NULL,
    last_heartbeat_at TIMESTAMPTZ NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 创建索引
CREATE INDEX idx_hosts_ip ON hosts(ip_address);
CREATE INDEX idx_hosts_hostname ON hosts(hostname);
CREATE INDEX idx_hosts_last_heartbeat ON hosts(last_heartbeat_at);

-- ============================================================
-- 创建表：templates (模板元数据表)
-- ============================================================
CREATE TABLE IF NOT EXISTS templates (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name VARCHAR(255) NOT NULL,
    file_type VARCHAR(20) NOT NULL,
    minio_object_name VARCHAR(255) NOT NULL,
    llm_prompt_template TEXT,
    status VARCHAR(20) NOT NULL DEFAULT 'parsing',
    error_message TEXT,
    rule_count INT DEFAULT 0,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 创建索引
CREATE INDEX idx_templates_status ON templates(status);
CREATE INDEX idx_templates_created ON templates(created_at);

-- ============================================================
-- 创建表：baseline_rules (基线规则表)
-- ============================================================
CREATE TABLE IF NOT EXISTS baseline_rules (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    template_id UUID NOT NULL REFERENCES templates(id) ON DELETE CASCADE,
    title VARCHAR(255) NOT NULL,
    check_content TEXT NOT NULL,
    fix_content TEXT NOT NULL,
    generated_check_script TEXT,
    generated_fix_script TEXT,
    check_script_version INT DEFAULT 0,
    fix_script_version INT DEFAULT 0,
    script_status VARCHAR(20) DEFAULT 'pending',
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 创建索引
CREATE INDEX idx_rules_template ON baseline_rules(template_id);
CREATE INDEX idx_rules_status ON baseline_rules(script_status);

-- ============================================================
-- 创建表：task_logs (执行日志表)
-- ============================================================
CREATE TABLE IF NOT EXISTS task_logs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    task_group_id UUID NOT NULL,
    rule_id UUID NOT NULL REFERENCES baseline_rules(id),
    host_id UUID NOT NULL REFERENCES hosts(id),
    task_type VARCHAR(20) NOT NULL,
    status VARCHAR(20) NOT NULL,
    script_content TEXT,
    script_version INT,
    stdout TEXT,
    stderr TEXT,
    exit_code INT,
    healing_id UUID,
    started_at TIMESTAMPTZ,
    finished_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 创建索引
CREATE INDEX idx_task_logs_group ON task_logs(task_group_id);
CREATE INDEX idx_task_logs_rule ON task_logs(rule_id);
CREATE INDEX idx_task_logs_host ON task_logs(host_id);
CREATE INDEX idx_task_logs_status ON task_logs(status);
CREATE INDEX idx_task_logs_created ON task_logs(created_at);

-- ============================================================
-- 创建表：llm_configs (LLM 配置表)
-- ============================================================
CREATE TABLE IF NOT EXISTS llm_configs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    api_key_encrypted TEXT NOT NULL,
    api_key_masked VARCHAR(50) NOT NULL,
    base_url VARCHAR(500) NOT NULL,
    model_name VARCHAR(100) NOT NULL DEFAULT 'qwen-plus',
    is_active BOOLEAN NOT NULL DEFAULT true,
    last_test_status VARCHAR(20),
    last_test_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 创建索引
CREATE INDEX idx_llm_configs_active ON llm_configs(is_active);

-- ============================================================
-- 创建表：script_versions (脚本版本表)
-- ============================================================
CREATE TABLE IF NOT EXISTS script_versions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    rule_id UUID NOT NULL REFERENCES baseline_rules(id),
    script_type VARCHAR(10) NOT NULL,
    version INT NOT NULL,
    script_content TEXT NOT NULL,
    generation_source VARCHAR(20) NOT NULL,
    llm_prompt_used TEXT,
    llm_response_raw TEXT,
    minio_object_name VARCHAR(255),
    is_current BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 创建索引
CREATE INDEX idx_script_versions_rule ON script_versions(rule_id);
CREATE INDEX idx_script_versions_type ON script_versions(script_type);
CREATE INDEX idx_script_versions_current ON script_versions(is_current);

-- ============================================================
-- 创建表：self_healing_logs (自愈日志表)
-- ============================================================
CREATE TABLE IF NOT EXISTS self_healing_logs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    original_task_id UUID NOT NULL REFERENCES task_logs(id),
    rule_id UUID NOT NULL REFERENCES baseline_rules(id),
    host_id UUID NOT NULL REFERENCES hosts(id),
    script_type VARCHAR(10) NOT NULL,
    status VARCHAR(20) NOT NULL,
    total_attempts INT NOT NULL DEFAULT 0,
    max_attempts INT NOT NULL DEFAULT 3,
    trigger_error TEXT NOT NULL,
    started_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    finished_at TIMESTAMPTZ
);

-- 创建索引
CREATE INDEX idx_healing_logs_task ON self_healing_logs(original_task_id);
CREATE INDEX idx_healing_logs_rule ON self_healing_logs(rule_id);
CREATE INDEX idx_healing_logs_status ON self_healing_logs(status);

-- ============================================================
-- 创建触发器：自动更新 updated_at
-- ============================================================

-- hosts 表
CREATE OR REPLACE FUNCTION update_updated_at_column()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = NOW();
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER update_hosts_updated_at
    BEFORE UPDATE ON hosts
    FOR EACH ROW
    EXECUTE FUNCTION update_updated_at_column();

-- templates 表
CREATE TRIGGER update_templates_updated_at
    BEFORE UPDATE ON templates
    FOR EACH ROW
    EXECUTE FUNCTION update_updated_at_column();

-- baseline_rules 表
CREATE TRIGGER update_baseline_rules_updated_at
    BEFORE UPDATE ON baseline_rules
    FOR EACH ROW
    EXECUTE FUNCTION update_updated_at_column();

-- llm_configs 表
CREATE TRIGGER update_llm_configs_updated_at
    BEFORE UPDATE ON llm_configs
    FOR EACH ROW
    EXECUTE FUNCTION update_updated_at_column();

-- ============================================================
-- 插入测试数据（可选）
-- ============================================================
-- 开发环境可以取消下面注释，生产环境请注释掉
-- INSERT INTO hosts (ip_address, hostname, os_type, agent_version, last_heartbeat_at)
-- VALUES ('192.168.1.10', 'test-server-01', 'linux', 'v1.0.0', NOW());
```

**Step 2: Verify file size**

```bash
wc -l /code/ai-benchmark/backend/scripts/init.sql
```

**Step 3: Commit**

```bash
git add backend/scripts/init.sql
git commit -m "feat(db): add complete init.sql with all 7 tables, indexes, and triggers"
```

---

## Phase 4: Frontend Project Structure

### Task 1.9: Create Frontend Directory Structure

**Files:**
- Create directories under `/code/ai-benchmark/frontend/`

**Step 1: Create all frontend directories**

```bash
cd /code/ai-benchmark/frontend

# Create directory structure
mkdir -p src/api
mkdir -p src/assets
mkdir -p src/components/base
mkdir -p src/components/common
mkdir -p src/composables
mkdir -p src/layouts
mkdir -p src/router
mkdir -p src/store
mkdir -p src/types
mkdir -p src/utils
mkdir -p src/views
```

**Step 2: Verify structure**

```bash
tree /code/ai-benchmark/frontend/src -d
```

**Step 3: Commit**

```bash
git add frontend/
git commit -m "feat(frontend): create project directory structure"
```

---

### Task 1.10: Create Frontend package.json

**Files:**
- Create: `/code/ai-benchmark/frontend/package.json`

**Step 1: Create package.json**

```json
{
  "name": "baseline-system-frontend",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "lint": "eslint . --ext .vue,.js,.jsx,.cjs,.mjs,.ts,.tsx,.cts,.mts",
    "type-check": "vue-tsc --noEmit"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "element-plus": "^2.4.0",
    "pinia": "^2.1.0",
    "vue": "^3.4.0",
    "vue-router": "^4.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "@vue/eslint-config-typescript": "^12.0.0",
    "@vue/tsconfig": "^0.5.0",
    "eslint": "^8.55.0",
    "eslint-plugin-vue": "^9.19.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0",
    "vue-tsc": "^1.8.0"
  }
}
```

**Step 2: Verify**

```bash
cat /code/ai-benchmark/frontend/package.json
```

**Step 3: Commit**

```bash
git add frontend/package.json
git commit -m "chore(frontend): add package.json with Vue 3, TypeScript, Element Plus dependencies"
```

---

### Task 1.11: Create Frontend Makefile

**Files:**
- Create: `/code/ai-benchmark/frontend/Makefile`

**Step 1: Create Makefile**

```makefile
# frontend/Makefile

.PHONY: all install build clean test docker

# Node 相关变量
NPM_CMD=npm

# Docker 相关变量
IMAGE_NAME=baseline-system/frontend
IMAGE_TAG?=latest

all: build

# 安装依赖
install:
	@echo "Installing dependencies..."
	$(NPM_CMD) install

# 构建静态文件
build: install
	@echo "Building frontend assets..."
	$(NPM_CMD) run build

# 清理构建产物
clean:
	@echo "Cleaning..."
	rm -rf dist node_modules

# 运行测试
test:
	@echo "Running tests..."
	$(NPM_CMD) run test

# 构建 Docker 镜像（调用 build.sh）
docker:
	@./build.sh $(IMAGE_NAME) $(IMAGE_TAG)
```

**Step 2: Verify**

```bash
cat /code/ai-benchmark/frontend/Makefile
```

**Step 3: Commit**

```bash
git add frontend/Makefile
git commit -m "chore(frontend): add Makefile with install, build, clean, test targets"
```

---

### Task 1.12: Create Frontend build.sh

**Files:**
- Create: `/code/ai-benchmark/frontend/build.sh`

**Step 1: Create build.sh**

```bash
#!/bin/bash
# frontend/build.sh

set -e

IMAGE_NAME=${1:-"baseline-system/frontend"}
IMAGE_TAG=${2:-"latest"}

# 基础镜像
BASE_IMAGE="node:18-alpine"

echo "Building Docker image ${IMAGE_NAME}:${IMAGE_TAG}..."

docker build -t ${IMAGE_NAME}:${IMAGE_TAG} \
    --build-arg BASE_IMAGE=${BASE_IMAGE} \
    -f Dockerfile .

echo "Build complete: ${IMAGE_NAME}:${IMAGE_TAG}"
```

**Step 2: Make executable**

```bash
chmod +x /code/ai-benchmark/frontend/build.sh
ls -la /code/ai-benchmark/frontend/build.sh
```

**Step 3: Commit**

```bash
git add frontend/build.sh
git commit -m "chore(frontend): add build.sh for Docker image building"
```

---

### Task 1.13: Create Frontend Dockerfile

**Files:**
- Create: `/code/ai-benchmark/frontend/Dockerfile`

**Step 1: Create Dockerfile**

```dockerfile
# frontend/Dockerfile

# --- Build Stage ---
ARG BASE_IMAGE=node:18-alpine
FROM ${BASE_IMAGE} AS builder

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .
RUN npm run build

# --- Final Stage ---
FROM nginx:1.23-alpine

# 复制构建好的静态文件到 Nginx 的 web 根目录
COPY --from=builder /app/dist /usr/share/nginx/html

# 复制 Nginx 配置文件
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

**Step 2: Verify**

```bash
cat /code/ai-benchmark/frontend/Dockerfile
```

**Step 3: Commit**

```bash
git add frontend/Dockerfile
git commit -m "chore(frontend): add multi-stage Dockerfile with Nginx"
```

---

### Task 1.14: Create Frontend nginx.conf

**Files:**
- Create: `/code/ai-benchmark/frontend/nginx.conf`

**Step 1: Create nginx.conf**

```nginx
# frontend/nginx.conf

server {
    listen 80;
    server_name _;

    # 前端静态资源
    root /usr/share/nginx/html;
    index index.html;

    # Vue Router History 模式支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 反向代理
    location /api/ {
        proxy_pass http://backend:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 文件上传大小限制
        client_max_body_size 50m;

        # 超时配置（LLM 调用可能较慢）
        proxy_connect_timeout 10s;
        proxy_read_timeout 300s;
        proxy_send_timeout 60s;
    }

    # 健康检查端点代理
    location /health {
        proxy_pass http://backend:8080;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
    }
}
```

**Step 2: Verify**

```bash
cat /code/ai-benchmark/frontend/nginx.conf
```

**Step 3: Commit**

```bash
git add frontend/nginx.conf
git commit -m "chore(frontend): add nginx.conf with API reverse proxy and Vue Router support"
```

---

## Phase 5: Agent Project Structure

### Task 1.15: Create Agent Directory Structure

**Files:**
- Create directories under `/code/ai-benchmark/agent/`

**Step 1: Create all agent directories**

```bash
cd /code/ai-benchmark/agent

# Create directory structure
mkdir -p cmd/agent
mkdir -p internal/config
mkdir -p internal/asset
mkdir -p internal/client
mkdir -p internal/executor
mkdir -p dist
```

**Step 2: Verify structure**

```bash
tree /code/ai-benchmark/agent -d
```

**Step 3: Commit**

```bash
git add agent/
git commit -m "feat(agent): create project directory structure"
```

---

### Task 1.16: Create Agent Makefile

**Files:**
- Create: `/code/ai-benchmark/agent/Makefile`

**Step 1: Create Makefile**

```makefile
# agent/Makefile

.PHONY: all build clean test upload

# Go 相关变量
BINARY_NAME=baseline-agent

# 交叉编译目标
TARGETS=linux/amd64 linux/arm64

all: build

# 在 Docker 容器中进行交叉编译
build:
	@echo "Cross-compiling Agent in Docker container..."
	docker run --rm -v "$(PWD)":/usr/src/myapp -w /usr/src/myapp golang:1.20-alpine sh -c ' \
	for target in $(TARGETS); do \
		GOOS=$$(echo $$target | cut -d'/' -f1) GOARCH=$$(echo $$target | cut -d'/' -f2) go build -v -o ./dist/$(BINARY_NAME)-$$GOOS-$$GOARCH ./cmd/agent; \
	done'

# 清理构建产物
clean:
	@echo "Cleaning..."
	rm -rf dist

# 运行测试
test:
	@echo "Running tests..."
	go test ./...

# 上传构建产物到 MinIO（调用 build.sh）
upload:
	@./build.sh
```

**Step 2: Verify**

```bash
cat /code/ai-benchmark/agent/Makefile
```

**Step 3: Commit**

```bash
git add agent/Makefile
git commit -m "chore(agent): add Makefile with cross-compilation targets"
```

---

### Task 1.17: Create Agent build.sh

**Files:**
- Create: `/code/ai-benchmark/agent/build.sh`

**Step 1: Create build.sh**

```bash
#!/bin/bash
# agent/build.sh

set -e

# MinIO 配置（从环境变量读取）
MINIO_ENDPOINT=${MINIO_ENDPOINT:-"http://localhost:9000"}
MINIO_ACCESS_KEY=${MINIO_ACCESS_KEY:-"minio_admin"}
MINIO_SECRET_KEY=${MINIO_SECRET_KEY:-"a_third_strong_secret_password"}
MINIO_BUCKET="agent-artifacts"

# 1. 执行编译
make build

# 2. 配置 MinIO 客户端（mc）
# 假设 mc 已经安装在系统中
echo "Configuring MinIO client..."
mc alias set myminio ${MINIO_ENDPOINT} ${MINIO_ACCESS_KEY} ${MINIO_SECRET_KEY}

# 3. 确保 Bucket 存在
mc mb myminio/${MINIO_BUCKET} --ignore-existing

# 4. 上传所有构建产物
echo "Uploading artifacts to MinIO bucket: ${MINIO_BUCKET}..."
mc cp --recursive dist/ myminio/${MINIO_BUCKET}/

echo "Upload complete."
```

**Step 2: Make executable**

```bash
chmod +x /code/ai-benchmark/agent/build.sh
ls -la /code/ai-benchmark/agent/build.sh
```

**Step 3: Commit**

```bash
git add agent/build.sh
git commit -m "chore(agent): add build.sh with MinIO upload"
```

---

## Phase 6: gRPC Protocol Buffers

### Task 1.18: Create Protobuf Definition

**Files:**
- Create: `/code/ai-benchmark/agent/pkg/api/v1/agent_comm.proto`

**Step 1: Create agent_comm.proto**

```protobuf
// agent/pkg/api/v1/agent_comm.proto
// Agent 与后端 gRPC 通讯协议定义
// 版本：V1.6

syntax = "proto3";

package agent_comm.v1;

option go_package = "baseline-system/pkg/api/v1";

// Agent 服务定义
service AgentService {
    // Agent 注册
    rpc Register(RegisterRequest) returns (RegisterResponse);
    // 心跳上报
    rpc Heartbeat(HeartbeatRequest) returns (HeartbeatResponse);
    // 命令执行（双向流）
    rpc ExecuteCommand(stream CommandRequest) returns (stream CommandResult);
}

// ============================================================
// 注册相关
// ============================================================

message RegisterRequest {
    string host_id = 1;          // Agent 唯一标识（留空则服务端生成）
    AssetInfo asset_info = 2;    // 资产信息
    string auth_token = 3;       // 认证 Token
}

message RegisterResponse {
    bool success = 1;
    string host_id = 2;          // 服务端分配的 host_id
    string message = 3;
}

message AssetInfo {
    string ip_address = 1;
    string hostname = 2;
    string os_type = 3;
    string os_version = 4;
    string arch = 5;
    string agent_version = 6;
}

// ============================================================
// 心跳相关
// ============================================================

message HeartbeatRequest {
    string host_id = 1;
    int64 timestamp = 2;
}

message HeartbeatResponse {
    bool success = 1;
    string message = 2;
}

// ============================================================
// 命令执行相关
// ============================================================

message CommandRequest {
    oneof request {
        CommandExecute execute = 1;    // 执行命令
        CommandResult result = 2;      // 执行结果
    }
}

message CommandExecute {
    string task_id = 1;
    string rule_id = 2;
    string host_id = 3;
    string script_content = 4;
    int32 timeout_seconds = 5;
}

message CommandResult {
    string task_id = 1;
    string host_id = 2;
    int32 exit_code = 3;
    string stdout = 4;
    string stderr = 5;
    bool is_final = 6;                 // 是否为最终结果
}
```

**Step 2: Verify**

```bash
cat /code/ai-benchmark/agent/pkg/api/v1/agent_comm.proto
```

**Step 3: Commit**

```bash
git add agent/pkg/api/v1/agent_comm.proto
git commit -m "feat(grpc): add agent_comm.proto with Register, Heartbeat, ExecuteCommand RPCs"
```

---

## Phase 7: .gitignore Files

### Task 1.19: Create Root .gitignore

**Files:**
- Create: `/code/ai-benchmark/.gitignore`

**Step 1: Create .gitignore**

```gitignore
# .gitignore - 自动化基线检查与自愈系统

# ======================
# 通用
# ======================
.env
*.log
*.tmp
*.swp
*~
.DS_Store
Thumbs.db

# ======================
# Backend (Go)
# ======================
backend/backend
backend/*.exe
backend/*.test
backend/*.out
backend/vendor/
backend/go.sum

# ======================
# Frontend (Node/Vue)
# ======================
frontend/node_modules/
frontend/dist/
frontend/.env.local
frontend/.env.*.local
frontend/npm-debug.log*
frontend/yarn-debug.log*
frontend/yarn-error.log*

# ======================
# Agent (Go)
# ======================
agent/dist/*
!agent/dist/.gitkeep
agent/*.exe
agent/*.test
agent/*.out

# ======================
# Docker
# ======================
docker-compose.override.yml

# ======================
# IDE
# ======================
.idea/
.vscode/
*.iml

# ======================
# 构建产物
# ======================
*.mod
*.sum
```

**Step 2: Create .gitkeep for dist directories**

```bash
touch /code/ai-benchmark/agent/dist/.gitkeep
```

**Step 3: Commit**

```bash
git add .gitignore agent/dist/.gitkeep
git commit -m "chore: add comprehensive .gitignore for Go, Vue, and Docker projects"
```

---

## Phase 8: Root Level README

### Task 1.20: Create README.md

**Files:**
- Create: `/code/ai-benchmark/README.md`

**Step 1: Create README.md**

```markdown
# 自动化基线检查与自愈系统 (Automated Baseline Check and Self-Healing System)

> **版本**: 2.0.0
> **状态**: 开发中

## 项目概述

一个面向运维和安全工程师的内部管理平台，通过结合大语言模型（LLM）的智能解析能力和 Agent 的自动化执行能力，简化基线配置的检查与修复流程。

**核心功能**：
- 上传基线文档（PDF、Word、YAML）→ LLM 解析为检查/修复规则
- 在服务器上部署 Agent 执行自动化基线检查
- 自愈功能：LLM 自动修复失败的脚本并重试

## 技术栈

| 组件 | 技术 |
|------|------|
| **后端** | Go 1.20+, Gin (REST API), gRPC (Agent 通讯) |
| **前端** | Vue 3 (Composition API), TypeScript, Vite, Pinia, Element Plus |
| **Agent** | Go (交叉编译：linux/amd64, linux/arm64) |
| **数据库** | PostgreSQL 14 |
| **缓存** | Redis 7 |
| **存储** | MinIO (文件/对象存储) |

## 快速开始

### 1. 准备环境

```bash
# 确保已安装 Docker 20.10+ 和 Docker Compose 2.0+
docker --version
docker-compose --version
```

### 2. 配置环境变量

```bash
cp .env.example .env
# 编辑 .env，修改密码和密钥为安全值
```

### 3. 构建镜像

```bash
# 构建后端
cd backend && ./build.sh && cd ..

# 构建前端
cd frontend && ./build.sh && cd ..
```

### 4. 启动服务

```bash
docker-compose up -d
```

### 5. 验证部署

```bash
# 检查所有容器状态
docker-compose ps

# 检查后端健康状态
curl http://localhost:8080/health

# 访问前端页面
# 浏览器打开 http://localhost
```

## 项目结构

```
.
├── backend/                 # Go 后端服务
│   ├── cmd/server/         # 入口程序
│   ├── config/             # 配置管理
│   ├── internal/           # 内部包（handler, service, repository 等）
│   ├── pkg/                # 公共包
│   ├── scripts/            # 数据库脚本
│   └── Makefile
├── frontend/               # Vue 3 前端
│   ├── src/
│   │   ├── api/           # API 层
│   │   ├── components/    # 组件
│   │   ├── composables/   # Composition API
│   │   ├── store/         # Pinia 状态管理
│   │   ├── types/         # TypeScript 类型
│   │   ├── utils/         # 工具函数
│   │   └── views/         # 页面
│   └── Makefile
├── agent/                  # Go Agent
│   ├── cmd/agent/         # 入口程序
│   ├── internal/          # 内部包
│   ├── dist/              # 编译产物
│   └── Makefile
├── docker-compose.yml      # Docker 编排配置
├── .env.example           # 环境变量模板
└── docs/                   # 文档
    └── plans/             # 实现计划
```

## 设计文档

所有设计文档位于 `baseline_system_design_v2.1/` 目录：

- `prd_design_v2.1_complete.md` - 产品需求文档
- `backend_detailed_design_v2.1_complete.md` - 后端详细设计
- `frontend_detailed_design_v2.1_complete.md` - 前端详细设计
- `database_structure_design_v2.1_complete.md` - 数据库设计
- `agent_detailed_design_v2.1_complete.md` - Agent 设计
- `infrastructure_design_v2.1_complete.md` - 基础设施设计
- `communication_structure_design_v2.1_complete.md` - 通讯协议设计
- `build_system_design_v2.1_complete.md` - 构建系统设计

## 开发指南

### 后端开发

```bash
cd backend

# 运行测试
make test

# 构建
make build

# 清理
make clean
```

### 前端开发

```bash
cd frontend

# 安装依赖
make install

# 开发服务器
npm run dev

# 构建
make build

# 类型检查
npm run type-check
```

### Agent 开发

```bash
cd agent

# 交叉编译
make build

# 运行测试
make test
```

## 部署

详细部署步骤请参考设计文档中的 `infrastructure_design_v2.1_complete.md`。

## License

内部项目，未授权。
```

**Step 2: Verify**

```bash
cat /code/ai-benchmark/README.md
wc -l /code/ai-benchmark/README.md
```

**Step 3: Commit**

```bash
git add README.md
git commit -m "docs: add comprehensive README with quick start and project structure"
```

---

## Final Verification

### Task 1.21: Verify Complete Project Structure

**Step 1: Display complete project tree**

```bash
cd /code/ai-benchmark
tree -L 4 -d -I 'node_modules|dist'
```

Expected output should show:
```
/code/ai-benchmark
├── agent/
│   ├── cmd/agent/
│   ├── dist/
│   ├── internal/
│   │   ├── asset/
│   │   ├── client/
│   │   ├── config/
│   │   └── executor/
│   └── pkg/api/v1/
├── backend/
│   ├── cmd/server/
│   ├── config/
│   ├── internal/
│   │   ├── api/
│   │   ├── fileparser/
│   │   ├── grpc_server/
│   │   ├── ipdetect/
│   │   ├── llm/
│   │   ├── model/
│   │   ├── repository/
│   │   ├── service/
│   │   └── storage/
│   ├── pkg/api/v1/
│   └── scripts/
├── baseline_system_design_v2.1/
├── docs/plans/
├── frontend/
│   └── src/
│       ├── api/
│       ├── assets/
│       ├── components/
│       ├── composables/
│       ├── layouts/
│       ├── router/
│       ├── store/
│       ├── types/
│       ├── utils/
│       └── views/
├── .env
├── .env.example
├── .gitignore
├── AGENTS.md
├── docker-compose.yml
└── README.md
```

**Step 2: Count all created files**

```bash
find /code/ai-benchmark -type f ! -path '*/.git/*' ! -path '*/baseline_system_design_v2.1/*' | wc -l
```

**Step 3: Verify key files exist**

```bash
ls -la /code/ai-benchmark/{.env.example,docker-compose.yml,README.md,AGENTS.md,.gitignore}
ls -la /code/ai-benchmark/backend/{Makefile,build.sh,Dockerfile,config/config.yaml,scripts/init.sql}
ls -la /code/ai-benchmark/frontend/{Makefile,build.sh,Dockerfile,package.json,nginx.conf}
ls -la /code/ai-benchmark/agent/{Makefile,build.sh,pkg/api/v1/agent_comm.proto}
```

---

## Summary

**Total Tasks:** 21
**Expected Commits:** 21 (one per logical unit)
**Key Deliverables:**
- ✅ Root: `.env.example`, `docker-compose.yml`, `.gitignore`, `README.md`
- ✅ Backend: Directory structure, Makefile, build.sh, Dockerfile, config.yaml, init.sql (7 tables)
- ✅ Frontend: Directory structure, package.json, Makefile, build.sh, Dockerfile, nginx.conf
- ✅ Agent: Directory structure, Makefile, build.sh, agent_comm.proto
- ✅ gRPC: Complete Protobuf definition with Register, Heartbeat, ExecuteCommand RPCs

**Next Steps:** After completing Task 1.1, proceed to **Task 1.2: Backend Configuration Module** (implement `backend/config/config.go` and `backend/internal/ipdetect/detector.go`).