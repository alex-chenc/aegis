# Aegis V5.8 设计-实现偏差修复 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 v5.8 设计文档与代码实现之间的全部28项偏差，使代码与设计文档完全一致。

**Architecture:** 分4层递进实施（L1基础层→L2核心层→L3服务层→L4表现层），每层有验证关卡。L1对齐Proto/DB/Model，L2修复Builder/Agent/Server核心逻辑，L3完善API Server业务逻辑，L4补全前端功能。

**Tech Stack:** Go 1.25, gRPC/Protobuf, GORM/PostgreSQL, Vue 3 + Element Plus + TypeScript, MinIO, Docker

---

## File Structure

### L1 基础层

| 操作 | 文件路径 | 职责 |
|------|----------|------|
| Modify | `proto/builder_comm.proto` | 新增 GetPackageBuildStatus/ReviewBuild RPC + 消息 |
| Modify | `proto/api_server_comm.proto` | SyncAgentConfig 改 repeated + 新增 ReportCorrelationAlert RPC |
| Modify | `proto/agent_comm.proto` | CommandRequest oneof 新增 DetectionPackageCommand/AllowlistUpdate |
| Create | `api-server/migrations/008_detection_package_v5.8_fix.sql` | 审核字段、角色权限表、变更原因字段 |
| Modify | `api-server/internal/model/detection_package.go` | 新增 ReviewedBy/ReviewedAt/StatusAwaitingReview 等 |
| Create | `api-server/internal/model/role_permission.go` | RolePermission 模型 |
| Create | `api-server/internal/repository/role_repo.go` | 角色权限仓库 |

### L2 核心层

| 操作 | 文件路径 | 职责 |
|------|----------|------|
| Modify | `builder/internal/service/builder_service.go` | SHA256真实计算、校验、审核、日志上传、Schema/HookSummary生成 |
| Modify | `builder/internal/grpc/server.go` | 新增 GetPackageBuildStatus/ReviewBuild gRPC handler |
| Modify | `builder/Dockerfile` | 注入 BUILDER_IMAGE_DIGEST |
| Create | `agent/internal/dynpkg/manifest.go` | PackageManifest/PluginManifest 解析与校验 |
| Create | `agent/internal/dynpkg/allowlist.go` | HookAllowlist 加载、校验、匹配 |
| Create | `agent/internal/dynpkg/state.go` | 状态机定义与转换 |
| Create | `agent/internal/dynpkg/storage.go` | 本地状态持久化 |
| Create | `agent/internal/dynpkg/ratelimiter.go` | 三级限速器 |
| Modify | `agent/internal/dynpkg/manager.go` | 精简为编排层，调用拆分模块 |
| Modify | `agent/internal/ebpf/plugin/loader.go` | perf buffer fallback |
| Create | `agent/internal/correlation/spec.go` | CorrelationSpec/CorrelationClause 解析 |
| Create | `agent/internal/correlation/finding.go` | AtomicFinding/CorrelationAlert/EvidenceItem 定义 |
| Create | `agent/internal/correlation/process_context.go` | ProcessContext + TreeKey 计算 |
| Modify | `agent/internal/correlation/engine.go` | 精简为引擎核心 |
| Modify | `agent/cmd/agent/main.go` | alert 上报接通 gRPC |
| Modify | `server/internal/grpc_server/api_server_impl.go` | 状态转发改 gRPC + DetectionPackageCommand/AllowlistUpdate 下发 |

### L3 服务层

| 操作 | 文件路径 | 职责 |
|------|----------|------|
| Modify | `api-server/internal/service/detection_package_service.go` | 版本降级防护、回滚、审核 |
| Modify | `api-server/internal/api/handler/detection_package_handler.go` | 新增审核/回滚 API handler |
| Modify | `api-server/internal/api/router.go` | 角色中间件 + 新增路由 |
| Modify | `api-server/internal/grpc/builder_client.go` | 类型安全改造 |
| Modify | `api-server/internal/grpc/client.go` | 新增 ReportCorrelationAlert 客户端方法 |
| Create | `api-server/internal/middleware/role.go` | 角色权限中间件 |

### L4 表现层

| 操作 | 文件路径 | 职责 |
|------|----------|------|
| Create | `frontend/src/composables/useRole.ts` | 角色权限 composable |
| Modify | `frontend/src/api/detection-packages.ts` | 新增审核/回滚/告警 API |
| Modify | `frontend/src/api/ebpf-hooks.ts` | 新增变更历史 API |
| Modify | `frontend/src/views/detection/DetectionPackages/PackageDetail.vue` | 新增告警证据/版本历史 tab |
| Modify | `frontend/src/views/detection/DetectionPackages/components/BuildReviewPanel.vue` | 审核按钮+日志下载 |
| Modify | `frontend/src/views/detection/DetectionPackages/components/CodeEditorPanel.vue` | YAML 语法校验 |
| Modify | `frontend/src/views/settings/EBPFHooks/index.vue` | 变更历史折叠面板 |

---

## Task 1: L1-Proto — 更新 builder_comm.proto

**Files:**
- Modify: `proto/builder_comm.proto`

- [ ] **Step 1: 新增 GetPackageBuildStatus/ReviewBuild RPC 和消息**

在 `BuilderService` 中新增 RPC，在文件末尾新增消息。同时在 `GetBuilderInfoResponse` 中新增 `builder_image_digest`、`llvm_version`、`libbpf_version` 字段。在 `StartBuild` RPC 上方添加注释标注命名偏差。

```protobuf
// NOTE: 设计文档中命名为 StartPackageBuild，保持当前命名以兼容现有客户端
rpc StartBuild(StartBuildRequest) returns (StartBuildResponse);
rpc GetPackageBuildStatus(GetPackageBuildStatusRequest) returns (GetPackageBuildStatusResponse);
rpc ReviewBuild(ReviewBuildRequest) returns (ReviewBuildResponse);

message GetPackageBuildStatusRequest {
  string package_id = 1;
  string version = 2;
  string build_id = 3;
}

message GetPackageBuildStatusResponse {
  string build_id = 1;
  string status = 2;
  string error_message = 3;
  int32 progress_percent = 4;
}

message ReviewBuildRequest {
  string build_id = 1;
  string package_id = 2;
  string version = 3;
  bool approved = 4;
  string comment = 5;
  string reviewer = 6;
}

message ReviewBuildResponse {
  bool success = 1;
  string message = 2;
  string new_status = 3;
}
```

在 `GetBuilderInfoResponse` 末尾新增：
```protobuf
  string builder_image_digest = 8;
  string llvm_version = 9;
  string libbpf_version = 10;
```

- [ ] **Step 2: 重新生成 protobuf Go 代码**

Run: `cd /code/ai-benchmark && make proto 2>&1 || (cd builder && protoc --go_out=. --go-grpc_out=. proto/builder_comm.proto 2>&1)`

Expected: 无错误

- [ ] **Step 3: Commit**

```bash
git add proto/builder_comm.proto builder/pkg/api/v1/
git commit -m "feat(proto): add GetPackageBuildStatus/ReviewBuild RPCs to builder_comm.proto"
```

---

## Task 2: L1-Proto — 更新 api_server_comm.proto

**Files:**
- Modify: `proto/api_server_comm.proto`

- [ ] **Step 1: 修改 SyncAgentConfigRequest 为 repeated + 新增 ReportCorrelationAlert RPC**

修改 `SyncAgentConfigRequest`：
```protobuf
message SyncAgentConfigRequest {
  string host_id = 1;
  repeated AgentConfig configs = 2;
}

message AgentConfig {
  string config_type = 1;
  string config_json = 2;
}
```

修改 `ReportDetectionPackageStatusRequest` 为 repeated：
```protobuf
message ReportDetectionPackageStatusRequest {
  repeated DetectionPackageHostStatus statuses = 1;
}
```

新增 RPC 和消息：
```protobuf
rpc ReportCorrelationAlert(ReportCorrelationAlertRequest) returns (ReportCorrelationAlertResponse);

message ReportCorrelationAlertRequest {
  string agent_id = 1;
  string package_id = 2;
  string correlation_rule_id = 3;
  string severity = 4;
  string evidence_chain_json = 5;
  string event_data_json = 6;
}

message ReportCorrelationAlertResponse {
  bool accepted = 1;
}
```

- [ ] **Step 2: 重新生成 protobuf Go 代码**

Run: `cd /code/ai-benchmark && make proto 2>&1`

- [ ] **Step 3: Commit**

```bash
git add proto/api_server_comm.proto api-server/pkg/api/v1/ server/pkg/api/v1/
git commit -m "feat(proto): add ReportCorrelationAlert RPC, make SyncAgentConfig/ReportStatus repeated"
```

---

## Task 3: L1-Proto — 更新 agent_comm.proto

**Files:**
- Modify: `proto/agent_comm.proto`

- [ ] **Step 1: 在 CommandRequest oneof 中新增 DetectionPackageCommand 和 AllowlistUpdate**

```protobuf
message CommandRequest {
  oneof command {
    CommandExecute execute = 1;
    CommandResult result = 2;
    RuleUpdate rule_update = 3;
    BlockCommand block = 4;
    ConfigSync config_sync = 5;
    DetectionPackageCommand detection_package_command = 6;
    AllowlistUpdate allowlist_update = 7;
  }
}

message DetectionPackageCommand {
  string action = 1;
  string package_id = 2;
  string version = 3;
  string package_url = 4;
  string signature_url = 5;
  int64 package_size = 6;
  string reason = 7;
}

message AllowlistUpdate {
  string version = 1;
  string allowlist_json = 2;
}
```

- [ ] **Step 2: 重新生成 protobuf Go 代码**

Run: `cd /code/ai-benchmark && make proto 2>&1`

- [ ] **Step 3: Commit**

```bash
git add proto/agent_comm.proto server/pkg/api/v1/ agent/pkg/api/v1/
git commit -m "feat(proto): add DetectionPackageCommand/AllowlistUpdate to CommandRequest oneof"
```

---

## Task 4: L1-DB — 新增数据库迁移

**Files:**
- Create: `api-server/migrations/008_detection_package_v5.8_fix.sql`

- [ ] **Step 1: 编写迁移 SQL**

```sql
-- 008_detection_package_v5.8_fix.sql
-- V5.8 设计偏差修复：审核字段、角色权限表、变更原因

ALTER TABLE detection_packages ADD COLUMN IF NOT EXISTS reviewed_by VARCHAR(64);
ALTER TABLE detection_packages ADD COLUMN IF NOT EXISTS reviewed_at TIMESTAMP;

ALTER TABLE detection_package_builds ADD COLUMN IF NOT EXISTS reviewed_by VARCHAR(64);
ALTER TABLE detection_package_builds ADD COLUMN IF NOT EXISTS reviewed_at TIMESTAMP;
ALTER TABLE detection_package_builds ADD COLUMN IF NOT EXISTS review_comment TEXT;

ALTER TABLE ebpf_hook_allowlist_configs ADD COLUMN IF NOT EXISTS change_reason TEXT;

CREATE TABLE IF NOT EXISTS role_permissions (
    id SERIAL PRIMARY KEY,
    user_id VARCHAR(64) NOT NULL,
    role VARCHAR(32) NOT NULL CHECK (role IN ('security_analyst', 'security_developer', 'admin')),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_id)
);

CREATE TABLE IF NOT EXISTS allowlist_change_history (
    id SERIAL PRIMARY KEY,
    version BIGINT NOT NULL REFERENCES ebpf_hook_allowlist_configs(version),
    operator VARCHAR(64) NOT NULL,
    change_reason TEXT,
    diff_json TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

INSERT INTO role_permissions (user_id, role)
SELECT DISTINCT operator, 'admin' FROM detection_package_operations
WHERE operator IS NOT NULL AND operator != ''
ON CONFLICT (user_id) DO NOTHING;
```

- [ ] **Step 2: Commit**

```bash
git add api-server/migrations/008_detection_package_v5.8_fix.sql
git commit -m "feat(db): add migration 008 for review fields, role_permissions, allowlist history"
```

---

## Task 5: L1-Model — 更新模型 + 新增 RolePermission

**Files:**
- Modify: `api-server/internal/model/detection_package.go`
- Create: `api-server/internal/model/role_permission.go`
- Create: `api-server/internal/repository/role_repo.go`

- [ ] **Step 1: 更新 DetectionPackage 模型**

在 `DetectionPackage` struct 新增：
```go
ReviewedBy *string `json:"reviewed_by" gorm:"column:reviewed_by"`
ReviewedAt *time.Time `json:"reviewed_at" gorm:"column:reviewed_at"`
```

在 `DetectionPackageBuild` struct 新增：
```go
ReviewedBy *string `json:"reviewed_by" gorm:"column:reviewed_by"`
ReviewedAt *time.Time `json:"reviewed_at" gorm:"column:reviewed_at"`
ReviewComment *string `json:"review_comment" gorm:"column:review_comment"`
```

在 `EBPFHookAllowlistConfig` struct 新增：
```go
ChangeReason *string `json:"change_reason" gorm:"column:change_reason"`
```

新增状态常量：
```go
const (
    StatusAwaitingReview = "awaiting_review"
    StatusReviewRejected = "review_rejected"
    OperationTypeReview  = "review"
)
```

- [ ] **Step 2: 创建 RolePermission 模型**

创建 `api-server/internal/model/role_permission.go`：
```go
package model

import "time"

type RolePermission struct {
    ID        uint      `json:"id" gorm:"primaryKey;autoIncrement"`
    UserID    string    `json:"user_id" gorm:"column:user_id;uniqueIndex;not null"`
    Role      string    `json:"role" gorm:"column:role;not null"`
    CreatedAt time.Time `json:"created_at" gorm:"column:created_at"`
    UpdatedAt time.Time `json:"updated_at" gorm:"column:updated_at"`
}

func (RolePermission) TableName() string {
    return "role_permissions"
}

const (
    RoleSecurityAnalyst   = "security_analyst"
    RoleSecurityDeveloper = "security_developer"
    RoleAdmin             = "admin"
)
```

- [ ] **Step 3: 创建 RoleRepo**

创建 `api-server/internal/repository/role_repo.go`：
```go
package repository

import (
    "api-server/internal/model"
    "gorm.io/gorm"
)

type RoleRepo struct {
    db *gorm.DB
}

func NewRoleRepo(db *gorm.DB) *RoleRepo {
    return &RoleRepo{db: db}
}

func (r *RoleRepo) GetRole(userID string) (string, error) {
    var rp model.RolePermission
    err := r.db.Where("user_id = ?", userID).First(&rp).Error
    if err != nil {
        if err == gorm.ErrRecordNotFound {
            return model.RoleSecurityAnalyst, nil
        }
        return "", err
    }
    return rp.Role, nil
}

func (r *RoleRepo) SetRole(userID, role string) error {
    var rp model.RolePermission
    err := r.db.Where("user_id = ?", userID).First(&rp).Error
    if err == gorm.ErrRecordNotFound {
        return r.db.Create(&model.RolePermission{UserID: userID, Role: role}).Error
    }
    if err != nil {
        return err
    }
    rp.Role = role
    return r.db.Save(&rp).Error
}

func (r *RoleRepo) HasPermission(role, operation string) bool {
    permissions := map[string][]string{
        model.RoleSecurityAnalyst:   {"view", "draft", "ai_generate"},
        model.RoleSecurityDeveloper: {"view", "draft", "ai_generate", "build", "review", "sign"},
        model.RoleAdmin:             {"view", "draft", "ai_generate", "build", "review", "sign", "enable", "disable", "uninstall", "rollback", "allowlist"},
    }
    ops, ok := permissions[role]
    if !ok {
        return false
    }
    for _, op := range ops {
        if op == operation {
            return true
        }
    }
    return false
}
```

- [ ] **Step 4: Commit**

```bash
git add api-server/internal/model/detection_package.go api-server/internal/model/role_permission.go api-server/internal/repository/role_repo.go
git commit -m "feat(model): add review fields, RolePermission model, RoleRepo with permission matrix"
```

---

## Task 6: L1-验证 — 编译检查 Proto + Model

- [ ] **Step 1: 编译全部 Go 服务**

Run: `cd /code/ai-benchmark/api-server && go build ./... 2>&1 && cd /code/ai-benchmark/server && go build ./... 2>&1 && cd /code/ai-benchmark/builder && go build ./... 2>&1 && cd /code/ai-benchmark/agent && go build ./... 2>&1`

Expected: 编译通过。修复因 Proto 变更导致的编译错误（Unimplemented 方法签名等）。

- [ ] **Step 2: Commit 任何修复**

```bash
git add -A && git commit -m "fix: resolve compilation errors from L1 proto/model changes"
```

---

## Task 7: L2-Builder — SHA256 真实计算 + Builder Image Digest

**Files:**
- Modify: `builder/internal/service/builder_service.go`

- [ ] **Step 1: 新增 computeSHA256 函数**

新增 import `"crypto/sha256"` 和 `"io"`，新增：
```go
func computeSHA256(filePath string) (string, error) {
    f, err := os.Open(filePath)
    if err != nil {
        return "", err
    }
    defer f.Close()
    h := sha256.New()
    if _, err := io.Copy(h, f); err != nil {
        return "", err
    }
    return fmt.Sprintf("%x", h.Sum(nil)), nil
}
```

- [ ] **Step 2: 在 StartBuild 中计算 unsigned package SHA256**

在 `createTarGz` 成功后、上传 MinIO 之前插入：
```go
sha256Hash, err := computeSHA256(packageFile)
if err != nil {
    result.Status = "failed"
    result.ErrorMessage = fmt.Sprintf("compute sha256: %v", err)
    return result, nil
}
result.UnsignedPackageSHA256 = sha256Hash
```

- [ ] **Step 3: 在 SignPackage 中计算 signed package SHA256**

替换 `PackageSHA256: "placeholder"` 为：
```go
sha256Hash, err := computeSHA256(localPath)
if err != nil {
    return &SignResult{Success: false, Message: fmt.Sprintf("compute sha256: %v", err)}, nil
}
// ... later ...
PackageSHA256: sha256Hash,
```

- [ ] **Step 4: 修复 Builder Image Digest**

替换 `result.BuilderImageDigest = "sha256:placeholder"` 为：
```go
result.BuilderImageDigest = os.Getenv("BUILDER_IMAGE_DIGEST")
```

- [ ] **Step 5: Commit**

```bash
git add builder/internal/service/builder_service.go
git commit -m "fix(builder): compute real SHA256 for packages, read BUILDER_IMAGE_DIGEST from env"
```

---

## Task 8: L2-Builder — 构建输入校验

**Files:**
- Modify: `builder/internal/service/builder_service.go`

- [ ] **Step 1: 新增 validateBuildInput 函数**

新增 import `"regexp"` 和 `"gopkg.in/yaml.v3"`：
```go
var forbiddenHelpers = []*regexp.Regexp{
    regexp.MustCompile(`bpf_probe_read_kernel`),
    regexp.MustCompile(`bpf_override_return`),
    regexp.MustCompile(`bpf_setsockopt`),
    regexp.MustCompile(`bpf_sk_redirect`),
}

func (s *BuilderService) validateBuildInput(req BuildRequest) error {
    if req.PackageID == "" || req.Version == "" || req.EBPFSource == "" {
        return fmt.Errorf("package_id, version, and ebpf_source are required")
    }
    if req.PackageMetadataJSON != "" {
        var manifest struct {
            SchemaVersion string `yaml:"schema_version"`
            PackageID     string `yaml:"package_id"`
            Version       string `yaml:"version"`
        }
        if err := yaml.Unmarshal([]byte(req.PackageMetadataJSON), &manifest); err != nil {
            return fmt.Errorf("invalid package.yaml: %w", err)
        }
        if manifest.SchemaVersion == "" || manifest.PackageID == "" || manifest.Version == "" {
            return fmt.Errorf("package.yaml must have schema_version, package_id, version")
        }
        if manifest.PackageID != req.PackageID {
            return fmt.Errorf("package.yaml package_id (%s) != request package_id (%s)", manifest.PackageID, req.PackageID)
        }
    }
    if req.HookPlanYAML != "" {
        var hookPlan struct {
            Hooks []struct {
                AttachType string `yaml:"attach_type"`
                Attach     string `yaml:"attach"`
            } `yaml:"hooks"`
        }
        if err := yaml.Unmarshal([]byte(req.HookPlanYAML), &hookPlan); err != nil {
            return fmt.Errorf("invalid hook_plan: %w", err)
        }
        for i, h := range hookPlan.Hooks {
            if h.AttachType == "" || h.Attach == "" {
                return fmt.Errorf("hook[%d]: attach_type and attach are required", i)
            }
        }
    }
    for _, re := range forbiddenHelpers {
        if re.MatchString(req.EBPFSource) {
            return fmt.Errorf("forbidden BPF helper call: %s", re.String())
        }
    }
    return nil
}
```

- [ ] **Step 2: 在 StartBuild 开头调用校验**

在 `buildDir` 创建之后、写源码之前插入：
```go
if err := s.validateBuildInput(req); err != nil {
    return &BuildResult{
        BuildID:      req.BuildID,
        Status:       "failed",
        ErrorMessage: fmt.Sprintf("validation: %v", err),
    }, nil
}
```

- [ ] **Step 3: Commit**

```bash
git add builder/internal/service/builder_service.go
git commit -m "feat(builder): add build input validation - YAML parse, hook plan check, forbidden BPF helpers"
```

---

## Task 9: L2-Builder — 人工审核流程 + Build log 上传

**Files:**
- Modify: `builder/internal/service/builder_service.go`

- [ ] **Step 1: 修改 StartBuild 完成后状态为 awaiting_review**

将 `result.Status = "success"` 改为 `result.Status = "awaiting_review"`

- [ ] **Step 2: 捕获编译日志并上传 MinIO**

修改 `compileBPF` 返回 stdout/stderr：
```go
func (s *BuilderService) compileBPF(source, output, transport, arch string) (string, error) {
    cmd := exec.Command("clang", "-O2", "-g", "-target", "bpf",
        "-D__TARGET_ARCH_"+arch,
        "-DTRANSPORT_"+transport,
        "-c", source, "-o", output)
    cmd.Dir = s.workDir
    var stdout, stderr bytes.Buffer
    cmd.Stdout = &stdout
    cmd.Stderr = &stderr
    err := cmd.Run()
    return stdout.String() + stderr.String(), err
}
```

在 `BuilderService` struct 新增 `buildLogs map[string]string`、`buildStatuses map[string]string`、`mu sync.Mutex`。在 `NewBuilderService` 中初始化。

在 `StartBuild` 中收集日志并上传：
```go
var buildLog string
perfLog, err := s.compileBPF(sourceFile, perfObj, "perf", req.TargetArch)
buildLog += perfLog + "\n"
// ... same for ringbuf ...
s.mu.Lock()
s.buildLogs[req.BuildID] = buildLog
s.buildStatuses[req.BuildID] = result.Status
s.mu.Unlock()

logKey := fmt.Sprintf("detection-packages/%s/%s/build.log", req.PackageID, req.Version)
logFile := filepath.Join(buildDir, "build.log")
os.WriteFile(logFile, []byte(buildLog), 0644)
if err := s.minioClient.UploadFile(ctx, "aegis-builds", logKey, logFile); err != nil {
    result.BuildLogTail = buildLog
} else {
    result.BuildLogObjectKey = logKey
}
const maxTail = 4096
tail := buildLog
if len(tail) > maxTail {
    tail = tail[len(tail)-maxTail:]
}
result.BuildLogTail = tail
```

- [ ] **Step 3: 新增 ReviewBuild 方法**

```go
func (s *BuilderService) ReviewBuild(ctx context.Context, buildID, packageID, version string, approved bool, comment, reviewer string) error {
    s.mu.Lock()
    defer s.mu.Unlock()
    status, exists := s.buildStatuses[buildID]
    if !exists {
        return fmt.Errorf("build %s not found", buildID)
    }
    if status != "awaiting_review" {
        return fmt.Errorf("build %s status is %s, not awaiting_review", buildID, status)
    }
    if approved {
        s.buildStatuses[buildID] = "success"
    } else {
        s.buildStatuses[buildID] = "review_rejected"
    }
    return nil
}
```

- [ ] **Step 4: SignPackage 前置条件检查**

在 `SignPackage` 开头新增：
```go
s.mu.Lock()
status, exists := s.buildStatuses[req.BuildID]
s.mu.Unlock()
if !exists || status != "success" {
    return &SignResult{Success: false, Message: fmt.Sprintf("build %s not approved (status: %s)", req.BuildID, status)}, nil
}
```

- [ ] **Step 5: Commit**

```bash
git add builder/internal/service/builder_service.go
git commit -m "feat(builder): add awaiting_review status, build log upload, ReviewBuild, sign precondition"
```

---

## Task 10: L2-Builder — Event Schema + HookSummary 生成

**Files:**
- Modify: `builder/internal/service/builder_service.go`

- [ ] **Step 1: 新增 Event Schema 生成**

新增 import `"encoding/json"`。在 `StartBuild` 编译成功后、打包前新增：
```go
if req.PackageMetadataJSON != "" {
    var manifest struct {
        EventSchema interface{} `yaml:"event_schema"`
    }
    if err := yaml.Unmarshal([]byte(req.PackageMetadataJSON), &manifest); err == nil {
        if manifest.EventSchema != nil {
            schemaJSON, _ := json.Marshal(manifest.EventSchema)
            result.EventSchemaJSON = string(schemaJSON)
        }
    }
}
```

- [ ] **Step 2: 新增 HookSummary 生成**

在 `StartBuild` 编译成功后新增：
```go
if req.HookPlanYAML != "" {
    var hookPlan struct {
        Hooks []struct {
            Name       string `yaml:"name"`
            AttachType string `yaml:"attach_type"`
            Attach     string `yaml:"attach"`
        } `yaml:"hooks"`
    }
    if err := yaml.Unmarshal([]byte(req.HookPlanYAML), &hookPlan); err == nil {
        for _, h := range hookPlan.Hooks {
            result.HookSummary = append(result.HookSummary, HookSummary{
                HookType:       h.AttachType,
                AttachPoint:    h.Attach,
                ProgramSection: h.Name,
                RiskLevel:      assessRiskLevel(h.AttachType),
            })
        }
    }
}
```

新增辅助函数：
```go
func assessRiskLevel(attachType string) string {
    switch attachType {
    case "tracepoint":
        return "low"
    case "kprobe", "kretprobe":
        return "medium"
    case "lsm", "xdp", "tc":
        return "high"
    default:
        return "unknown"
    }
}
```

- [ ] **Step 3: Commit**

```bash
git add builder/internal/service/builder_service.go
git commit -m "feat(builder): generate EventSchema from package.yaml, HookSummary from hook_plan"
```

---

## Task 11: L2-Builder — GetPackageBuildStatus + GetBuilderInfo 增强 + gRPC handler + Dockerfile

**Files:**
- Modify: `builder/internal/service/builder_service.go`
- Modify: `builder/internal/grpc/server.go`
- Modify: `builder/Dockerfile`

- [ ] **Step 1: 新增 GetPackageBuildStatus 方法**

```go
type BuildStatusInfo struct {
    BuildID         string
    Status          string
    ErrorMessage    string
    ProgressPercent int32
}

func (s *BuilderService) GetPackageBuildStatus(ctx context.Context, packageID, version, buildID string) (*BuildStatusInfo, error) {
    s.mu.Lock()
    defer s.mu.Unlock()
    status, exists := s.buildStatuses[buildID]
    if !exists {
        return nil, fmt.Errorf("build %s not found", buildID)
    }
    info := &BuildStatusInfo{BuildID: buildID, Status: status}
    if status == "failed" {
        info.ErrorMessage = "build failed"
    } else if status == "awaiting_review" || status == "success" {
        info.ProgressPercent = 100
    }
    return info, nil
}
```

- [ ] **Step 2: 增强 GetBuilderInfo — LLVM/libbpf 版本解析**

新增：
```go
func parseClangVersion(raw string) string {
    re := regexp.MustCompile(`clang version (\d+\.\d+\.\d+)`)
    m := re.FindStringSubmatch(raw)
    if len(m) > 1 {
        return m[1]
    }
    return strings.TrimSpace(raw)
}

func parseLibbpfVersion(raw string) string {
    re := regexp.MustCompile(`libbpf (\d+\.\d+\.\d+)`)
    m := re.FindStringSubmatch(raw)
    if len(m) > 1 {
        return m[1]
    }
    return "unknown"
}
```

在 `GetBuilderInfo` 中使用解析后的版本号。

- [ ] **Step 3: 新增 gRPC handler**

在 `builder/internal/grpc/server.go` 中新增 `GetPackageBuildStatus` 和 `ReviewBuild` handler。

- [ ] **Step 4: 更新 Dockerfile 注入 BUILDER_IMAGE_DIGEST**

```dockerfile
ARG BUILDER_IMAGE_DIGEST=""
ENV BUILDER_IMAGE_DIGEST=${BUILDER_IMAGE_DIGEST}
```

- [ ] **Step 5: Commit**

```bash
git add builder/internal/service/builder_service.go builder/internal/grpc/server.go builder/Dockerfile
git commit -m "feat(builder): add GetPackageBuildStatus, ReviewBuild gRPC handlers, LLVM/libbpf version parsing"
```

---

## Task 12: L2-Builder — 编译验证

- [ ] **Step 1: 编译 builder**

Run: `cd /code/ai-benchmark/builder && go build ./... 2>&1`

Expected: 编译通过。修复任何编译错误。

- [ ] **Step 2: Commit 任何修复**

```bash
git add -A && git commit -m "fix(builder): resolve compilation errors from L2 builder changes"
```

---

## Task 13: L2-Agent — 拆分 dynpkg 模块 (manifest.go + allowlist.go)

**Files:**
- Create: `agent/internal/dynpkg/manifest.go`
- Create: `agent/internal/dynpkg/allowlist.go`

- [ ] **Step 1: 创建 manifest.go**

从 `manager.go` 提取 `PackageManifest`、`PluginManifest`、`PluginHook`、`PackageArtifact`、`PackageLimits` 类型和 `ParseManifest`/`ValidateManifest`/`ParseManifestFromFile` 函数。

- [ ] **Step 2: 创建 allowlist.go**

从 `manager.go` 提取 `HookAllowlist` 类型和 `AllowlistChecker`（含 `Update`/`Get`/`IsHookAllowed`/`CheckPackage` 方法）。

- [ ] **Step 3: Commit**

```bash
git add agent/internal/dynpkg/manifest.go agent/internal/dynpkg/allowlist.go
git commit -m "refactor(agent): extract manifest.go and allowlist.go from dynpkg manager"
```

---

## Task 14: L2-Agent — 拆分 dynpkg 模块 (state.go + storage.go + ratelimiter.go)

**Files:**
- Create: `agent/internal/dynpkg/state.go`
- Create: `agent/internal/dynpkg/storage.go`
- Create: `agent/internal/dynpkg/ratelimiter.go`

- [ ] **Step 1: 创建 state.go — 状态机**

定义 `PackageState` 枚举（15个状态）、`validTransitions` 映射、`StateMachine` struct（含 `Current`/`CanTransition`/`Transition`/`History` 方法）。

- [ ] **Step 2: 创建 storage.go — 本地状态持久化**

定义 `PackageStateRecord`、`PackageStorage`（含 `SaveState`/`LoadState`/`ListInstalled` 方法，JSON 文件持久化）。

- [ ] **Step 3: 创建 ratelimiter.go — 三级限速器**

定义 `RateLimiter`（含 `Allow(pluginID, eventType, pid)`/`UpdateLimits`/`checkExceed` 方法，per-plugin/type/pid 令牌桶，超限30s触发 disableCallback）。

- [ ] **Step 4: Commit**

```bash
git add agent/internal/dynpkg/state.go agent/internal/dynpkg/storage.go agent/internal/dynpkg/ratelimiter.go
git commit -m "feat(agent): add state machine, storage, and rate limiter modules for dynpkg"
```

---

## Task 15: L2-Agent — 重构 manager.go 使用拆分模块

**Files:**
- Modify: `agent/internal/dynpkg/manager.go`

- [ ] **Step 1: 重构 Manager struct 使用新模块**

替换内联字段为 `allowlistChecker *AllowlistChecker`、`storage *PackageStorage`、`rateLimiter *RateLimiter`。在 `InstalledPackage` 中新增 `stateMachine *StateMachine`。

- [ ] **Step 2: 重构 Install 方法使用状态机**

每个阶段使用 `pkg.stateMachine.Transition()` 转换，使用 `m.allowlistChecker.CheckPackage()` 校验，使用 `m.storage.SaveState()` 持久化。

- [ ] **Step 3: 新增 DisableByPolicy 方法**

停止 correlation engine spec、移除 sigma rules、关闭 eBPF reader、detach links、状态转换 → disabled_by_policy、持久化、上报。

- [ ] **Step 4: 新增 disableByRateLimit 回调**

状态转换 → disabled_by_rate、unload plugin、持久化、上报。

- [ ] **Step 5: 删除 manager.go 中已迁移的类型定义**

删除 `PackageManifest`、`PluginManifest`、`PluginHook`、`PackageArtifact`、`HookAllowlist`（已在 manifest.go 和 allowlist.go 中定义）。

- [ ] **Step 6: Commit**

```bash
git add agent/internal/dynpkg/manager.go
git commit -m "refactor(agent): rewrite dynpkg manager to use state machine, storage, rate limiter modules"
```

---

## Task 16: L2-Agent — perf buffer fallback

**Files:**
- Modify: `agent/internal/ebpf/plugin/loader.go`

- [ ] **Step 1: 实现 ringbuf 优先 + perf fallback**

新增 `ringbufOrPerfReader` 接口和 `ringbufReaderWrapper`/`perfReaderWrapper` 包装器。在 `LoadPlugin` 中先尝试 ringbuf，失败后 fallback 到 perf buffer。在 `PluginInstance` 中新增 `Transport string` 字段记录使用的 transport。

- [ ] **Step 2: Commit**

```bash
git add agent/internal/ebpf/plugin/loader.go
git commit -m "feat(agent): implement ringbuf priority with perf buffer fallback in plugin loader"
```

---

## Task 17: L2-Agent — Correlation 模块拆分

**Files:**
- Create: `agent/internal/correlation/spec.go`
- Create: `agent/internal/correlation/finding.go`
- Create: `agent/internal/correlation/process_context.go`
- Modify: `agent/internal/correlation/engine.go`

- [ ] **Step 1: 创建 spec.go** — 提取 `CorrelationSpec`、`CorrelationClause`、`SequenceStep`、`AlertSpec`

- [ ] **Step 2: 创建 finding.go** — 提取 `AtomicFinding`、`CorrelationAlert`、`EvidenceItem`

- [ ] **Step 3: 创建 process_context.go** — 新增 `ProcessContext`（含 TreeKey 字段）和 `ComputeTreeKey` 函数

- [ ] **Step 4: 精简 engine.go** — 删除已迁移类型，修改 `getCorrelationKey` 支持 `by: pid_tree` 使用 TreeKey

- [ ] **Step 5: Commit**

```bash
git add agent/internal/correlation/
git commit -m "refactor(agent): split correlation module into spec, finding, process_context, engine"
```

---

## Task 18: L2-Agent — alert 上报接通 gRPC

**Files:**
- Modify: `agent/cmd/agent/main.go`

- [ ] **Step 1: 修改 alert callback 通过 ReportEvent 上报**

替换 `// TODO: Report alert to server via gRPC` 为实际 gRPC 调用，构造 `RuntimeEvent`（`event_type = "correlation_alert"`），通过 `grpcClient.ReportEvent` 上报。

- [ ] **Step 2: Commit**

```bash
git add agent/cmd/agent/main.go
git commit -m "feat(agent): wire correlation alert callback to gRPC ReportEvent"
```

---

## Task 19: L2-Server — 状态转发改 gRPC + DetectionPackageCommand/AllowlistUpdate 下发

**Files:**
- Modify: `server/internal/grpc_server/api_server_impl.go`

- [ ] **Step 1: 修改 forwardStatusToAPIServer 改用 gRPC**

删除 HTTP POST 实现，改为调用 api-server gRPC `ReportDetectionPackageStatus` RPC。

- [ ] **Step 2: 修改 InstallDetectionPackage 使用 DetectionPackageCommand oneof**

将当前通过 `ConfigSync` 下发改为通过 `CommandRequest.detection_package_command` 下发。

- [ ] **Step 3: 修改 SyncAgentConfig 中 allowlist 使用 AllowlistUpdate oneof**

当 `configType == "dynamic_ebpf_hook_allowlist"` 时，改用 `CommandRequest.allowlist_update`。

- [ ] **Step 4: Commit**

```bash
git add server/internal/grpc_server/api_server_impl.go
git commit -m "feat(server): switch status forwarding to gRPC, use DetectionPackageCommand/AllowlistUpdate oneof"
```

---

## Task 20: L2-验证 — 编译检查全部服务

- [ ] **Step 1: 编译全部 Go 服务**

Run: `cd /code/ai-benchmark/agent && go build ./... 2>&1 && cd /code/ai-benchmark/server && go build ./... 2>&1 && cd /code/ai-benchmark/builder && go build ./... 2>&1 && cd /code/ai-benchmark/api-server && go build ./... 2>&1 && cd /code/ai-benchmark/dc && go build ./... 2>&1`

Expected: 全部编译通过。

- [ ] **Step 2: Commit 任何修复**

```bash
git add -A && git commit -m "fix: resolve compilation errors from L2 changes"
```

---

## Task 21: L3-API — 版本降级防护 + 回滚

**Files:**
- Modify: `api-server/internal/service/detection_package_service.go`

- [ ] **Step 1: 添加 go-version 依赖**

Run: `cd /code/ai-benchmark/api-server && go get github.com/hashicorp/go-version`

- [ ] **Step 2: 在 EnablePackage 中添加版本降级防护**

获取当前 enabled 版本后，使用 `goversion.NewVersion` 比较，若新版本低于当前版本则拒绝（rollback 除外）。

- [ ] **Step 3: 新增 RollbackPackage 方法**

获取当前 enabled 版本标记 disabled，获取目标 signed 版本标记 enabled，通过 Server gRPC 下发 `DetectionPackageCommand{action: "rollback"}`，记录 operation。

- [ ] **Step 4: Commit**

```bash
git add api-server/internal/service/detection_package_service.go api-server/go.mod api-server/go.sum
git commit -m "feat(api-server): add version downgrade protection and RollbackPackage"
```

---

## Task 22: L3-API — 审核 API + 角色权限中间件

**Files:**
- Modify: `api-server/internal/api/handler/detection_package_handler.go`
- Create: `api-server/internal/middleware/role.go`
- Modify: `api-server/internal/api/router.go`

- [ ] **Step 1: 创建角色权限中间件**

创建 `api-server/internal/middleware/role.go`，实现 `RoleMiddleware(roleRepo *repository.RoleRepo, requiredOperation string) gin.HandlerFunc`。

- [ ] **Step 2: 新增审核 API handler**

在 `detection_package_handler.go` 中新增 `ReviewBuild` handler：校验 build status 为 `awaiting_review`，审核通过 → `success`，审核拒绝 → `review_rejected`。

- [ ] **Step 3: 新增回滚 API handler**

新增 `RollbackPackage` handler：调用 `service.RollbackPackage`。

- [ ] **Step 4: 新增告警查询 API handler**

新增 `ListPackageAlerts` handler：查询 `runtime_events` 中 `package_id` 匹配的 correlation_alert 事件。

- [ ] **Step 5: 新增构建日志下载 API handler**

新增 `GetBuildLog` handler：从 MinIO 获取 build.log 预签名 URL 或流式下载。

- [ ] **Step 6: 更新路由注册**

在 `router.go` 中新增路由并添加角色中间件：
- `POST /api/v1/detection/packages/builds/:id/review` → RoleMiddleware("review")
- `POST /api/v1/detection/packages/:id/rollback` → RoleMiddleware("rollback")
- `GET /api/v1/detection/packages/:id/alerts` → RoleMiddleware("view")
- `GET /api/v1/detection/packages/builds/:id/log` → RoleMiddleware("review")
- `GET /api/v1/settings/ebpf-hooks/allowlist/history` → RoleMiddleware("allowlist")

- [ ] **Step 7: Commit**

```bash
git add api-server/internal/middleware/role.go api-server/internal/api/handler/detection_package_handler.go api-server/internal/api/router.go
git commit -m "feat(api-server): add review/rollback/alert/log APIs, role permission middleware"
```

---

## Task 23: L3-API — builder_client 类型安全 + client.go 新增方法

**Files:**
- Modify: `api-server/internal/grpc/builder_client.go`
- Modify: `api-server/internal/grpc/client.go`

- [ ] **Step 1: builder_client 类型安全改造**

将 `StartBuild(ctx, interface{}) (interface{}, error)` 改为使用 `BuilderStartBuildRequest`/`BuilderStartBuildResponse` 结构体，消除 `map[string]interface{}`。

- [ ] **Step 2: client.go 新增 ReportCorrelationAlert 方法**

```go
func (c *ServerClient) ReportCorrelationAlert(ctx context.Context, req *pb.ReportCorrelationAlertRequest) (*pb.ReportCorrelationAlertResponse, error) {
    return c.client.ReportCorrelationAlert(ctx, req)
}
```

- [ ] **Step 3: Commit**

```bash
git add api-server/internal/grpc/builder_client.go api-server/internal/grpc/client.go
git commit -m "refactor(api-server): type-safe builder_client, add ReportCorrelationAlert to server client"
```

---

## Task 24: L3-验证 — 编译检查 API Server

- [ ] **Step 1: 编译 api-server**

Run: `cd /code/ai-benchmark/api-server && go build ./... 2>&1`

Expected: 编译通过。

- [ ] **Step 2: Commit 任何修复**

```bash
git add -A && git commit -m "fix: resolve compilation errors from L3 changes"
```

---

## Task 25: L4-Frontend — 角色权限 composable + API 扩展

**Files:**
- Create: `frontend/src/composables/useRole.ts`
- Modify: `frontend/src/api/detection-packages.ts`
- Modify: `frontend/src/api/ebpf-hooks.ts`

- [ ] **Step 1: 创建 useRole.ts composable**

```typescript
import { ref } from 'vue'

type Role = 'security_analyst' | 'security_developer' | 'admin'

const PERMISSIONS: Record<Role, string[]> = {
  security_analyst: ['view', 'draft', 'ai_generate'],
  security_developer: ['view', 'draft', 'ai_generate', 'build', 'review', 'sign'],
  admin: ['view', 'draft', 'ai_generate', 'build', 'review', 'sign', 'enable', 'disable', 'uninstall', 'rollback', 'allowlist'],
}

const currentRole = ref<Role>('security_analyst')

export function useRole() {
  const canOperate = (operation: string) => PERMISSIONS[currentRole.value].includes(operation)
  const setRole = (role: Role) => { currentRole.value = role }
  return { role: currentRole, canOperate, setRole }
}
```

- [ ] **Step 2: 扩展 detection-packages.ts API**

新增方法：
- `reviewBuild(buildId, data)` → POST `/detection/packages/builds/:id/review`
- `rollbackPackage(packageId, targetVersion)` → POST `/detection/packages/:id/rollback`
- `getPackageAlerts(packageId)` → GET `/detection/packages/:id/alerts`
- `getBuildLog(buildId)` → GET `/detection/packages/builds/:id/log`

- [ ] **Step 3: 扩展 ebpf-hooks.ts API**

新增方法：
- `getAllowlistHistory()` → GET `/settings/ebpf-hooks/allowlist/history`

- [ ] **Step 4: Commit**

```bash
git add frontend/src/composables/useRole.ts frontend/src/api/detection-packages.ts frontend/src/api/ebpf-hooks.ts
git commit -m "feat(frontend): add useRole composable, extend detection-packages and ebpf-hooks APIs"
```

---

## Task 26: L4-Frontend — PackageDetail 新增告警证据/版本历史 tab

**Files:**
- Modify: `frontend/src/views/detection/DetectionPackages/PackageDetail.vue`

- [ ] **Step 1: 新增"告警证据" tab**

在 `el-tabs` 中新增 pane，调用 `getPackageAlerts(packageId)` 获取 correlation alerts，每条 alert 展开显示 `EvidenceTimeline` 组件。

- [ ] **Step 2: 新增"版本历史" tab**

在 `el-tabs` 中新增 pane，表格显示版本号、状态、构建时间、签名时间、启用时间、操作人。每行操作列：仅管理员可见"回滚到此版本"按钮，回滚需二次确认弹窗。

- [ ] **Step 3: 绑定角色权限**

所有操作按钮绑定 `:disabled="!canOperate('build')"` 等。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/views/detection/DetectionPackages/PackageDetail.vue
git commit -m "feat(frontend): add alert evidence and version history tabs to PackageDetail"
```

---

## Task 27: L4-Frontend — BuildReviewPanel 审核按钮+日志下载

**Files:**
- Modify: `frontend/src/views/detection/DetectionPackages/components/BuildReviewPanel.vue`

- [ ] **Step 1: 新增审核按钮**

当 build status 为 `awaiting_review` 时，显示"审核通过"和"审核拒绝"按钮，审核需填写审核意见（`el-input type="textarea"`）。审核通过后签名按钮才可用。仅安全开发大师和管理员可见审核按钮。

- [ ] **Step 2: 新增日志下载按钮**

新增"下载完整日志"按钮，调用 `getBuildLog(buildId)` 获取日志下载链接。

- [ ] **Step 3: Commit**

```bash
git add frontend/src/views/detection/DetectionPackages/components/BuildReviewPanel.vue
git commit -m "feat(frontend): add review buttons and log download to BuildReviewPanel"
```

---

## Task 28: L4-Frontend — CodeEditorPanel YAML 校验

**Files:**
- Modify: `frontend/src/views/detection/DetectionPackages/components/CodeEditorPanel.vue`

- [ ] **Step 1: 安装 js-yaml 依赖**

Run: `cd /code/ai-benchmark/frontend && npm install js-yaml && npm install -D @types/js-yaml`

- [ ] **Step 2: 集成 YAML 语法校验**

在 `onChange` 时，若 `currentLang === 'yaml'`，调用 `js-yaml.load()` 校验。语法错误在编辑器下方红色提示。校验失败时保存按钮 disabled。

- [ ] **Step 3: Commit**

```bash
git add frontend/src/views/detection/DetectionPackages/components/CodeEditorPanel.vue frontend/package.json frontend/package-lock.json
git commit -m "feat(frontend): add YAML syntax validation to CodeEditorPanel"
```

---

## Task 29: L4-Frontend — EBPFHooks 变更历史

**Files:**
- Modify: `frontend/src/views/settings/EBPFHooks/index.vue`

- [ ] **Step 1: 新增变更历史折叠面板**

在页面底部新增 `el-collapse` 面板，调用 `ebpfHookApi.getAllowlistHistory()` 获取历史记录。表格列：操作人、操作时间、变更原因、变更内容（diff 展示）。默认折叠。

- [ ] **Step 2: Commit**

```bash
git add frontend/src/views/settings/EBPFHooks/index.vue
git commit -m "feat(frontend): add allowlist change history panel to EBPFHooks settings"
```

---

## Task 30: L4-验证 — 前端编译检查

- [ ] **Step 1: 前端类型检查**

Run: `cd /code/ai-benchmark/frontend && npm run type-check 2>&1`

Expected: 无类型错误。

- [ ] **Step 2: 前端 lint**

Run: `cd /code/ai-benchmark/frontend && npm run lint 2>&1`

Expected: 无 lint 错误。

- [ ] **Step 3: 前端构建**

Run: `cd /code/ai-benchmark/frontend && npm run build 2>&1`

Expected: 构建成功。

- [ ] **Step 4: Commit 任何修复**

```bash
git add -A && git commit -m "fix: resolve frontend type/lint/build errors from L4 changes"
```

---

## Task 31: 全量验证 — Docker Compose 构建启动

- [ ] **Step 1: Docker Compose 构建**

Run: `cd /code/ai-benchmark && docker compose build 2>&1`

Expected: 全部服务构建成功。

- [ ] **Step 2: Docker Compose 启动**

Run: `cd /code/ai-benchmark && docker compose up -d 2>&1`

Expected: 全部服务启动成功。

- [ ] **Step 3: 健康检查**

Run: `sleep 30 && curl -s http://localhost:8082/health 2>&1`

Expected: 返回 healthy 状态。

- [ ] **Step 4: Commit 任何修复**

```bash
git add -A && git commit -m "fix: resolve docker compose build/startup issues"
```

---

## 修复项与 Task 对照表

| # | 优先级 | 修复项 | Task |
|---|--------|--------|------|
| 1 | P0 | Package SHA256 placeholder → 真实计算 | 7 |
| 2 | P0 | Builder Image Digest placeholder | 7, 11 |
| 3 | P0 | 构建输入无校验 | 8 |
| 4 | P0 | Agent alert 上报未接 gRPC | 18 |
| 5 | P0 | DetectionPackageCommand 不在 CommandRequest oneof | 3, 19 |
| 6 | P1 | GetPackageBuildStatus RPC 缺失 | 1, 11 |
| 7 | P1 | 人工审核流程缺失 | 9 |
| 8 | P1 | 限速 RateLimiter 缺失 | 14, 15 |
| 9 | P1 | 版本降级防护缺失 | 21 |
| 10 | P1 | 版本回滚缺失 | 21, 22 |
| 11 | P1 | perf buffer fallback 缺失 | 16 |
| 12 | P1 | Build log 未上传 MinIO | 9 |
| 13 | P1 | Event Schema 未生成 | 10 |
| 14 | P1 | HookSummary 未从 BPF 对象提取 | 10 |
| 15 | P1 | ProcessContext.TreeKey 缺失 | 17 |
| 16 | P2 | StartBuild vs StartPackageBuild 命名 | 1 (注释标注) |
| 17 | P2 | SyncAgentConfig flat vs repeated | 2 |
| 18 | P2 | ReportDetectionPackageStatus 单条 vs repeated | 2 |
| 19 | P2 | GetBuilderInfo 缺字段 | 11 |
| 20 | P2 | Agent 模块未按设计拆分 | 13, 14, 15 |
| 21 | P2 | Correlation 模块未按设计拆分 | 17 |
| 22 | P2 | builder_client 用 interface{} | 23 |
| 23 | P3 | 用户角色权限控制 | 5, 22, 25, 26 |
| 24 | P3 | EvidenceTimeline 未在详情页使用 | 26 |
| 25 | P3 | 构建日志下载按钮 | 27 |
| 26 | P3 | 版本历史/回滚 UI | 26 |
| 27 | P3 | Allowlist 变更审计日志 | 4, 29 |
| 28 | P3 | CodeEditorPanel YAML 语法校验 | 28 |

## 实施完成总结 (2026-05-26)

### 全部 31 个 Task 已完成

- **Tasks 1-6** (L1-Proto/DB/Model): proto 更新、DB 迁移 008、模型更新、编译检查 ✅
- **Tasks 7-11** (L2-Builder): SHA256 真实计算、构建输入校验、人工审核、日志上传、Schema/HookSummary、gRPC handler ✅
- **Tasks 13-15** (L2-Agent): dynpkg 模块拆分、manager.go 重构、lifecycle.go 删除 ✅
- **Task 16** (L2-Agent): perf buffer fallback ✅
- **Task 17** (L2-Agent): Correlation 模块拆分 ✅
- **Task 18** (L2-Agent): alert 上报接通 gRPC ✅
- **Task 19** (L2-Server): 状态转发 gRPC + oneof + ReportCorrelationAlert ✅
- **Task 20** (L2-验证): 5 个 Go 服务编译通过 ✅
- **Task 21** (L3-API): 版本降级防护 + RollbackPackage ✅
- **Task 22** (L3-API): role 中间件 + 5 个新 handler + router 更新 ✅
- **Task 23** (L3-API): builder_client 类型安全 + ReportCorrelationAlert ✅
- **Task 24** (L3-验证): API Server 编译通过 ✅
- **Task 25** (L4-Frontend): useRole composable + API 扩展 ✅
- **Task 26** (L4-Frontend): PackageDetail 新增告警证据 tab + 版本历史 tab + 角色权限绑定 ✅
- **Task 27** (L4-Frontend): BuildReviewPanel 审核按钮+日志下载+签名按钮 ✅
- **Task 28** (L4-Frontend): CodeEditorPanel YAML 校验 ✅
- **Task 29** (L4-Frontend): EBPFHooks 变更历史面板 ✅
- **Task 30** (L4-验证): 前端 build 通过 ✅
- **Task 31** (全量验证): Docker Compose 构建启动 + API 测试 ✅

### 全量验证结果

**Docker 服务状态**: 全部 10 个服务 healthy
- api-server (8082/19093) ✅
- server (19090/19094) ✅
- dc (19092) ✅
- builder (19096) ✅
- frontend (8081) ✅
- postgres/redis/minio/kafka/zookeeper ✅

**API 测试**: 13 个端点组全部正常
- Health/Auth/Hosts/Alerts/Packages/Rules/BlockPolicies ✅
- eBPF Hooks Allowlist/History ✅
- Config/Notifications/CommandAudit/AuditLogs ✅
- 角色权限中间件 (review/rollback/view/allowlist) ✅

### 修复过程中的额外发现与修复

1. **Proto pb.go 生成与分布问题**: 同一 .proto 需为不同服务生成不同 go_package 的 pb.go，通过复制 proto 到 /tmp 修改 go_package 后分别生成解决
2. **api_server_comm.pb.go 运行时 panic**: raw descriptor 与代码不匹配导致 `slice bounds out of range [-4:]`，通过重新 protoc 生成修复
3. **role 中间件 key 不匹配**: auth 中间件设置 `auth_username`，role 中间件读取 `username`，已修复
4. **Docker 构建网络问题**: Alpine 镜像源和 Go proxy 被墙，已切换到阿里云镜像和 goproxy.cn
5. **前端 80 端口冲突**: 宿主机 nginx 占用 80 端口，docker-compose.yml 已改为只映射 8081:80
